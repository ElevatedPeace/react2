import React from "react";

const BottomRightMenu = (props) => {
  return (
    <div style={{ fontSize: "12px" }}>
      <p> {props.footerRightText} </p>
    </div>
  );
};

export default BottomRightMenu;
